import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // Opsional, gunakan jika ada styling global
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
