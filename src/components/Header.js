import React from "react";

const Header = () => {
  return (
    <header style={{ backgroundColor: "#ff5722", padding: "1rem", color: "white" }}>
      <h1>Food Store</h1>
    </header>
  );
};

export default Header;
